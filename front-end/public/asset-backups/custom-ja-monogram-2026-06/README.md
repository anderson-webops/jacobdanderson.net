# Custom JA Monogram Backup

This folder preserves the custom initials-based favicon assets that were active before restoring the original logo/favicon set.

- `site-icon.svg` was previously linked as `/icons/site-icon.svg`.
- `site.webmanifest` was previously served as `/site.webmanifest`.

The active site now uses the original `Logo Bo Light` favicon family and logo image restored from git history.
