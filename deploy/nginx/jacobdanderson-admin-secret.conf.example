# Install as /etc/nginx/snippets/jacobdanderson-admin-secret.conf with mode 0600.
# Replace the placeholder with the same strong random value used for
# PROJECT_ADMIN_PROXY_KEY in /etc/jacobdanderson/api.env.
proxy_set_header X-Portfolio-Admin-Key "replace-with-a-strong-random-value";
